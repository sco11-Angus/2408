# AI-2048 认知训练平台：从经典游戏到多模式认知训练的工程实践

> 一个融合经典 2048 玩法与认知训练功能的开源前端项目，支持 ADHD 专注模式、老年认知模式、多步撤销、训练报告生成。全程使用 CodeBuddy AI 辅助开发，零手写代码完成从需求到上线的完整交付。

---

## 1. 项目背景

2048 是一款风靡全球的数字合并游戏。我们在经典玩法的基础上，将其改造为**面向特定人群的认知训练工具**，核心创新点在于：

- **ADHD 专注模式**：10 分钟限时训练，用时间边界 + 正向反馈帮助 ADHD 人群进行持续性注意力训练
- **老年认知模式**：全局字号放大 20%，降低操作门槛，帮助老年人锻炼逻辑与记忆能力
- **训练报告系统**：每局结束后自动生成数据统计 + 表现总结 + 鼓励反馈 + 训练建议

技术栈：**纯前端（HTML + CSS + JavaScript）**，零框架依赖，单文件即可运行。

---

## 2. 项目架构

### 2.1 模块化设计

项目采用职责单一的模块化架构，每个 `.js` 文件只负责一个明确的功能域：

```
src/
├── game.js          ← 核心游戏逻辑（棋盘、移动、合并、胜负判定、撤销、音效）
├── modes.js         ← 训练模式配置（ADHD/普通/老年 三种模式参数）
├── render.js        ← 棋盘渲染与消息提示
├── input.js         ← 键盘 + 触屏输入控制
├── gameStats.js     ← 游戏统计数据记录
├── reportGenerator.js ← 训练报告规则生成器
├── reportModal.js   ← 报告弹窗渲染与交互
└── modeSelector.js  ← 模式选择首页
```

加载顺序在 `index.html` 中严格按依赖排列：

```html
<script src="src/modes.js"></script>          <!-- 模式配置，最先加载 -->
<script src="src/gameStats.js"></script>      <!-- 统计模块 -->
<script src="src/game.js"></script>           <!-- 核心逻辑 -->
<script src="src/reportGenerator.js"></script> <!-- 报告生成器 -->
<script src="src/render.js"></script>          <!-- 渲染 -->
<script src="src/reportModal.js"></script>     <!-- 报告弹窗 -->
<script src="src/modeSelector.js"></script>    <!-- 首页 -->
<script src="src/input.js"></script>           <!-- 输入控制 -->
```

### 2.2 核心模块详解

#### game.js — 核心游戏引擎（约 520 行）

这是整个项目的核心，实现了 2048 的完整游戏逻辑。

**棋盘与状态管理：**

```javascript
let board = [];        // 二维数组棋盘
let score = 0;         // 当前分数
let bestScore = 0;     // 历史最高分（持久化到 localStorage）
let size = 4;          // 棋盘边长（支持 3/4/5）
let undoStack = [];    // 多步撤销栈
```

**滑动合并算法（slideRow）：**

这是 2048 最核心的算法。对单行/列执行：去零 → 相邻相同合并 → 补零：

```javascript
function slideRow(row) {
    let arr = row.filter(v => v !== 0);           // 1. 移除所有空格
    for (let i = 0; i < arr.length - 1; i++) {
        if (arr[i] === arr[i + 1]) {               // 2. 相邻相同则合并
            arr[i] *= 2;
            score += arr[i];                       // 3. 累加分数
            playMergeSound(arr[i]);                // 4. 播放音效
            arr.splice(i + 1, 1);                  // 5. 移除被合并项
        }
    }
    while (arr.length < size) arr.push(0);         // 6. 补零到原有长度
    return arr;
}
```

四个方向的移动通过不同的数组旋转实现复用同一算法：

- `moveLeft()` — 每行直接调用 `slideRow()`
- `moveRight()` — 每行反转 → slideRow → 反转回来
- `moveUp()` — 每列转置为行 → slideRow → 写回
- `moveDown()` — 每列反转 → slideRow → 反转回来

**多步撤销（undoStack）：**

每次移动前保存完整棋盘快照到撤销栈：

```javascript
function snapshotBoard() {
    undoStack.push({
        board: board.map(row => [...row]),
        score: score
    });
}

function undoMove() {
    if (undoStack.length === 0) return false;
    const snap = undoStack.pop();
    board = snap.board.map(row => [...row]);
    score = snap.score;
    return true;
}
```

相比单步撤销（只保留一个快照），多步撤销栈可以回退到游戏中的任意历史状态。

**音效系统（Web Audio API）：**

不使用音频文件，直接在浏览器中合成音效：

```javascript
function playBeep(freq = 440, duration = 0.08, type = 'sine') {
    const osc = audioCtx.createOscillator();
    const gain = audioCtx.createGain();
    osc.type = type;
    osc.frequency.setValueAtTime(freq, audioCtx.currentTime);
    gain.gain.exponentialRampToValueAtTime(0.001, audioCtx.currentTime + duration);
    osc.connect(gain).connect(audioCtx.destination);
    osc.start();
    osc.stop(audioCtx.currentTime + duration);
}

// 合并音效随数字增大而升高音高
function playMergeSound(value) {
    const freq = 220 + Math.log2(value) * 55;
    playBeep(freq, 0.12, 'triangle');
}
```

#### modes.js — 训练模式配置

三种模式统一用对象描述，禁止在业务逻辑中硬编码：

```javascript
const GAME_MODES = {
    normal: { id: 'normal', name: '普通模式', boardSize: 4, undoLimit: Infinity },
    adhd:   { id: 'adhd', name: 'ADHD专注模式', boardSize: 4, targetTime: 600 },
    senior: { id: 'senior', name: '老年认知模式', boardSize: 4, largeFont: true }
};
```

这种配置化设计的优势在于：添加新模式只需增加一个对象，无需修改业务代码。

#### input.js — 输入控制

支持三套按键方案（方向键 / WASD / HJKL-Vim风格）+ 触屏滑动：

```javascript
const KEY_MAP = {
    'ArrowLeft': 'left', 'ArrowRight': 'right',
    'ArrowUp': 'up', 'ArrowDown': 'down',
    'a': 'left', 'd': 'right', 'w': 'up', 's': 'down',
    'h': 'left', 'l': 'right', 'k': 'up', 'j': 'down'
};
```

触屏通过 `touchstart/touchend` 计算滑动方向，阈值 30px 区分点击与滑动。

---

## 3. V1.2 训练报告系统（重点功能）

这是本次迭代的核心功能：每局游戏结束后自动弹出训练报告。

### 3.1 数据流设计

```
游戏结束（胜/败/ADHD超时）
    ↓
GameStats.finish() → 收集时长/步数/分数/最大数字
    ↓
ReportGenerator.generate() → 规则生成总结+鼓励+建议
    ↓
ReportModal.show() → 渲染弹窗
```

### 3.2 gameStats.js — 统计数据模块

独立封装的数据采集层，在游戏生命周期中埋点：

```javascript
const GameStats = {
    mode: 'normal',     // 训练模式ID
    startTime: 0,       // 开始时间戳
    moveCount: 0,       // 移动步数
    score: 0,           // 最终分数
    maxTile: 0,         // 最大数字

    start(modeId) { /* 游戏开始，重置所有计数 */ },
    recordMove() { this.moveCount++; },    // 每次移动 +1
    finish(score, maxTile) {               // 游戏结束，计算时长
        this.duration = Date.now() - this.startTime;
        return this.getReport();
    },
    formatDuration(ms) {                   // 格式化：8分32秒
        const min = Math.floor(ms / 60000);
        const sec = Math.floor((ms % 60000) / 1000);
        return min > 0 ? min + '分' + sec + '秒' : sec + '秒';
    }
};
```

埋点时机：
- `start()` — `game.initBoard()` 中调用
- `recordMove()` — `game.doMove()` 每次有效移动后调用
- `finish()` — `game.onGameEnd()` 统一回调中调用

### 3.3 reportGenerator.js — 报告生成器

采用**纯规则引擎**方式，不接入 AI API。设计上预留了接口，后续可无缝替换为 LLM 生成。

**表现总结的三维评价体系：**

| 维度 | 阈值 | 评价 |
|------|------|------|
| 时长 | <5分钟 | 完成了一次快速训练 |
|  | 5-15分钟 | 完成了一次较为完整的训练 |
|  | >15分钟 | 保持了较长时间的持续训练 |
| 步数 | <150 | 决策速度较快 |
|  | 150-300 | 节奏较为稳定 |
|  | >300 | 进行了充分思考 |
| 最大数字 | ≥512 | 已经掌握基础策略 |
|  | ≥1024 | 逻辑规划能力表现良好 |
|  | ≥2048 | 成功完成2048挑战！ |
|  | ≥4096 | 表现优秀，继续保持！ |

**训练建议按优先级匹配：**

```javascript
generateSuggestion(stats) {
    if (stats.moveCount < 150)        return '可以尝试提前规划数字布局。';
    if (stats.maxTile < 512)          return '建议尽量将最大数字固定在角落。';
    if (stats.maxTile >= 2048)        return '可以挑战更高分数，尝试完成4096。';
    return '继续保持稳定节奏。';       // 默认
}
```

**鼓励语随机抽取：**

```javascript
const encouragements = [
    '坚持训练会带来持续进步！',
    '今天也完成了一次脑力挑战！',
    '每一步思考都值得肯定！',
    '继续努力，下次一定会更进一步！',
    '保持专注，你正在不断成长！'
];
// 随机返回一句
```

### 3.4 reportModal.js — 报告弹窗

弹窗采用**纯 DOM 操作**，不依赖任何 UI 框架。核心设计：

```javascript
show(stats) {
    const report = ReportGenerator.generate(stats);
    const durationStr = GameStats.formatDuration(stats.duration);

    // 构建完整 HTML，insertAdjacentHTML 注入 body
    const html = '<div class="report-overlay">...弹窗内容...</div>';
    document.body.insertAdjacentHTML('beforeend', html);

    // requestAnimationFrame 触发入场动画
    requestAnimationFrame(() => {
        overlay.classList.add('report-overlay-show');
    });
}
```

弹窗展示：训练模式 / 时长 / 步数 / 最高分 / 最大数字 / 表现总结 / 鼓励 / 建议，底部两个按钮「重新开始」和「关闭（返回首页）」。

---

## 4. ADHD 专注模式设计

### 4.1 计时器实现

```javascript
function startTimer(seconds) {
    remainingTime = seconds;
    timerInterval = setInterval(() => {
        remainingTime--;
        updateTimerDisplay();
        if (remainingTime <= 0) {
            stopTimer();
            onTrainingComplete();  // → 触发训练报告
        }
    }, 1000);
}
```

最后 60 秒计时器变红色警告，增强时间紧迫感。

### 4.2 设计理念

| 设计点 | 实现 |
|--------|------|
| 时间框定 | 10 分钟硬性边界，比"无限玩"更易保持专注 |
| 正向反馈 | 超时显示 🏆「训练结束」而非「失败」 |
| 可控承诺 | 10 分钟是可完成的短周期，降低焦虑感 |
| 数据沉淀 | 每次训练记录数据，为后续 AI 分析积累样本 |

---

## 5. 测试与 CI/CD

### 5.1 单元测试

项目使用 Mocha + Chai 进行自动化测试，覆盖核心算法：

```
test/
├── game.test.js    ← initBoard、slideRow、moveLeft/Right/Up/Down、
│                        spawnTile、checkWin/checkLose、undoMove
├── input.test.js   ← 按键映射、Ctrl+Z 撤销
└── render.test.js  ← renderBoard 输出验证
```

### 5.2 GitHub Actions 流水线

```
quality.yml:  Lint → Test(18/20/22 矩阵) → Coverage → AI Review → 质量门禁
deploy.yml:   Build → Deploy to GitHub Pages
```

---

## 6. 在线体验

- 🎮 **在线地址**：[https://scoll-Angus.github.io/2408/](https://scoll-Angus.github.io/2408/)
- 📦 **源码仓库**：[https://gitlink.org.cn/YYYYY111111/2408.git](https://gitlink.org.cn/YYYYY111111/2408.git)

也可以直接下载 `index.html` 用浏览器打开，零依赖即可运行。

---

## 7. 总结

AI-2048 认知训练平台是一个工程化程度较高的前端实践项目，具备以下技术亮点：

1. **模块化架构** — 8 个职责单一的 JS 模块，按依赖顺序加载
2. **配置驱动** — 三种训练模式纯配置化，新增模式无需改业务代码
3. **规则引擎** — 训练报告采用可替换的规则生成器，预留 AI 接口
4. **多端支持** — 键盘（三套按键）+ 触屏滑动全覆盖
5. **算法复用** — 四个方向移动通过数组旋转复用同一 slideRow 算法
6. **零依赖运行** — 纯 HTML/CSS/JS，单文件即可在浏览器运行
7. **AI 辅助开发** — 全程使用 CodeBuddy 进行需求分析、架构设计、代码实现、迭代优化

> 项目从需求文档到可运行的 V1.2 版本（含 3 种训练模式、多步撤销、计时器、训练报告），全程通过对话式 AI 辅助完成，验证了 AI 编程助手在前端项目中的高效实践路径。
