# AI-2048 V1.3 实现说明文档

## 目标

接入大语言模型（Claude API），实现两个 AI 功能：

1. **智能教练**：游戏过程中实时提示策略
2. **AI 训练报告**：游戏结束后由 AI 分析生成个性化报告

---

## 现状

V1.2 已实现：

- 规则统计（时长、步数、最大数字、分数）
- 模板生成训练报告（`reportGenerator.js`）
- 报告弹窗（`reportModal.js`）

V1.3 在此基础上替换 + 新增。

---

## 功能一：智能教练（AI Coach）

### 触发方式

不自动触发（避免打断流程、避免 API 滥用）。

玩家主动点击「💡 教练提示」按钮触发。

### 交互流程

```
玩家点击「💡 教练提示」
↓
按钮变灰 + 显示「分析中…」
↓
读取当前棋盘状态
↓
调用 Claude API
↓
弹出提示气泡（Tooltip / 悬浮卡片）
↓
玩家关闭 / 自动 10 秒消失
```

### 发送给 AI 的信息

```javascript
{
  board: [[0,2,4,8], [16,32,0,0], ...],   // 当前 4×4（或 N×N）棋盘
  size: 4,
  score: 5216,
  maxTile: 1024,
  moveCount: 186,
  mode: "normal"
}
```

### Prompt 设计

```
你是一个 2048 游戏策略教练。

当前棋盘（行列从左上到右下）：
{board_text}

当前分数：{score}
最大数字：{maxTile}
已操作步数：{moveCount}

请给出：
1. 当前局面简评（1句）
2. 下一步建议方向（上/下/左/右），并说明理由（2句以内）
3. 中期战略提示（1句）

回答用中文，简洁直接，不超过80字。
```

### UI 位置

「💡 教练提示」按钮放在操作按钮区（新游戏 / 撤销 旁边）。

提示内容显示在棋盘下方的固定区域（非弹窗），避免遮挡棋盘。

### 频率限制

每局最多调用 **5 次**（防止 API 消耗过大）。

计数显示在按钮旁：`💡 教练提示（3/5）`

---

## 功能二：AI 训练报告

### 变化

V1.2：`reportGenerator.js` 用规则 + 模板生成报告。

V1.3：游戏结束后调用 Claude API，传入统计数据，返回个性化报告文字。

`reportGenerator.js` 改为 AI 版，兼容原有接口。

### 发送给 AI 的信息

```javascript
{
  mode: "normal",          // 训练模式
  duration: 512,           // 秒
  moveCount: 186,
  score: 5216,
  maxTile: 1024,
  boardSize: 4,
  isWin: false
}
```

### Prompt 设计

```
你是一个认知训练分析师。用户刚完成一局 2048 游戏。

训练数据：
- 模式：{mode}
- 时长：{duration}
- 步数：{moveCount}
- 得分：{score}
- 最大数字：{maxTile}
- 棋盘大小：{boardSize}×{boardSize}
- 是否获胜：{isWin}

请生成训练报告，包含：
1. 表现总结（2句，结合数据）
2. 鼓励反馈（1句，有温度）
3. 训练建议（1~2句，具体可执行）

用中文，总字数不超过 120 字，语气积极专业。
```

### 降级策略

API 调用失败（超时 / 无网络 / 无 Key）时，自动回退到 V1.2 规则生成报告，不影响用户体验。

报告区域显示小字提示：`（AI 服务暂时不可用，显示基础报告）`

### 报告弹窗变化

新增「AI 分析」标签，区分 AI 报告和规则报告。

加载时显示：`🤖 AI 正在分析中…`（骨架屏，不阻塞弹窗打开）

---

## API 接入方案

### 选型

使用 **Anthropic Claude API**（`claude-haiku-4-5-20251001`，成本低、响应快）。

### API Key 管理

纯前端项目，API Key 不能硬编码。

方案：用户首次使用时输入 Key，存入 `localStorage`。

```
首次点击「💡 教练提示」或游戏结束时
↓
检查 localStorage 是否有 key
↓
无 key → 弹出输入框 → 用户填入 → 保存到 localStorage
↓
后续直接使用
```

设置入口：页面右上角「⚙ 设置」图标，可清除或更新 Key。

### 请求封装

新增 `src/aiService.js`：

```javascript
// 统一封装所有 AI 调用
async function callClaude(prompt, maxTokens = 200) { ... }

async function getCoachHint(gameState) { ... }

async function generateAIReport(stats) { ... }
```

### CORS 问题

Claude API 支持直接从浏览器调用（CORS 已开放），无需代理服务器。

---

## 文件变动

### 新增

```
src/
├── aiService.js        # Claude API 调用封装
├── apiKeyModal.js      # API Key 输入弹窗
```

### 修改

```
src/
├── reportGenerator.js  # 改为 AI 生成 + 规则降级
├── reportModal.js      # 新增加载状态 / AI 标签
├── game.js             # 接入 coach 调用计数
index.html              # 新增「教练提示」按钮 / 设置入口
style.css               # 新增教练气泡、加载态样式
```

---

## 技术约束

- 不引入任何 npm 包 / 构建工具，保持纯 HTML+CSS+JS
- 不修改 2048 核心算法（`initBoard` / `move` / `merge`）
- AI 功能完全可选：无 Key 时游戏正常运行，报告降级为规则版
- 所有 AI 调用异步处理，不阻塞游戏主线程

---

## 验收标准

### 智能教练

- ✅ 点击按钮后显示加载态
- ✅ 收到回复后显示在棋盘下方
- ✅ 每局限制 5 次，超出后按钮禁用
- ✅ 无 API Key 时引导用户设置
- ✅ API 失败时显示友好错误提示

### AI 训练报告

- ✅ 游戏结束后弹出报告，AI 分析异步加载
- ✅ 弹窗先显示统计数据，AI 分析内容异步填入
- ✅ API 失败自动降级，不影响报告弹出
- ✅ AI 报告与规则报告有视觉区分

### 通用

- ✅ API Key 可设置 / 清除
- ✅ 无 Key 状态下游戏全功能正常
- ✅ 不影响撤销、模式选择、棋盘逻辑

---

## 后续可探索（V1.4+）

- 对局复盘：游戏结束后 AI 分析关键转折点
- 难度自适应：AI 根据训练数据建议调整棋盘大小 / 模式
- 历史趋势：多局数据持久化 + AI 长期进步分析
